// Changelog.cfg v1.0.0.1
// StorkDeliverySystem
// created: 01 Aug 2019
// updated: 24 Feb 2020

// ——————————————————————————————————————————————————
// ——— changelog ————————————————————————————————————
// ——————————————————————————————————————————————————

KERBALCHANGELOG
{
	showChangelog = True
	modName = StorkDeliverySystem
	VERSION
	{
		version = 0.0.4.0
		change = Mission Control, the Stork has landed! [KSP 1.7.3]
		change = License: CC BY-NC-SA 4.0
		change = converted to use Localization - translations welcomed through GitHub Push Request
		change = file structure and modernization
		change = added Kerbal Changelog
		change = added Readme
		change = created github repo
		change = created SpaceDock entry
		change = created CKAN entry
		change = created Curseforge entry
		change = created Forum post
		change = created .json
		change = modernized part.cfg
		change = BulkheadProfiles corrected to bulkheadProfiles
		change = ModuleToggleCrossfeed
		change = formating
		change = tweakscale stack (if TweakScale not installed won't affect anything)
		change = adjusted category: from structural to Payload
		change = many little changes to patches/parts.
	}
	VERSION
	{
		version = 0.0.3.0
		change = >>-- adoption by zer0Kerbal --<<
	}
	VERSION
	{
		version = 0.0.2.6
		change = -----0.2.6---
		change = updated version to KSP 1.3.0
	}
	VERSION
	{
		version = 0.0.2.5
		change = -----0.2.5---
		change = updated version to KSP 1.2
	}
	VERSION
	{
		version = 0.0.2.4
		change = -----0.2.4---
		change = updated version to KSP 1.1.2
	}
	VERSION
	{
		version = 0.0.2.3
		change = -----0.2.3---
	change = fixed attachment nodes vectors for KSP 1.0
	}
	VERSION
	{
		version = 0.0.2.2 
		change = -----0.2.2---
		change = updated required tech (CTT compatible)
		change = changed directory structure to be consistent with MFS mod
	}
	VERSION
	{
		version = 0.0.2.1
		change = -----0.21----
		change = fixed slight mesh glitch in medium legs
	}
	VERSION
	{
		version = 0.0.2.0
		change = -----0.2------
		change = moved up center of mass for legs to avoid situation when they reverted to horizontal position even from horizontal one
		change = slightly raised mass of every part to balance them against stock truss and landing legs mass (also joints are a little stornger now but you still need minimal amount of struts)
		change = fixed mesh problems
		change = added decoupler function to stork legs (in the middle point where you normally mount cargo) so you don't need to add additional separator/decoupler to them
		change = changed stork decoupler to two-side separator to enable clean separation (might be important if you reuse other parts of rocket)
	}
	VERSION
	{
		version = 0.0.1.0
		change = ------0.1------
		change = Initial release:
		change = 3 parts (short, medium frame with legs and bottom decoupler)
	}
}

// CC BY-NC-SA-4.0
// zer0Kerbal